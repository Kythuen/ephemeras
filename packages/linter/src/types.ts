export type PromptData = {
  preset: string
  features: string[]
  environment: string[]
  framework: string
  typescript: boolean
  commitHook: boolean
  commitMessage: boolean
  save: boolean
  install: boolean
  packageManager: string
}
